name: task_cli
description: >-
  Application CLI de gestion de tâches en Dart pur avec persistance JSON,
  tri par priorité ou échéance, et gestion de tâches urgentes.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
